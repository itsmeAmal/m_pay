package com.mobios.beet.controller;

import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/beet")
@CrossOrigin(origins = "localhost", maxAge = 3600)
public class RegistrationController {

	
	/*
	 * @PostMapping("/getDetails/mobileno") public List<Customer>
	 * getDetailsByMobileNo(@PathVariable (value = "mobileno") String mobileno){
	 * List<Customer> DetailsBymobieNo =
	 * RegistrationServeiceImpl.findDetailsByMobileNumber(mobileno); return
	 * DetailsBymobieNo; }
	 */
	
		
}
